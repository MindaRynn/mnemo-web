<h2>Log in</h2>

<%= react_component('LoginForm', props: {url: session_path(resource_name), csrfToken: csrf_token}, html_options: {class: 'form'}) %>

<%= link_to "Sign in with Facebook", user_facebook_omniauth_authorize_path %>

<%= render "devise/shared/links" %>
